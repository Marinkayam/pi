# Copy to .env.local for `vercel dev`. Never commit the real values.
# The API key lives ONLY here (local) and in Vercel Env Vars (production).
ANTHROPIC_API_KEY=sk-ant-your-key-here
MONITOR_KEY=choose-a-long-random-string
# Optional (auto-added by the Upstash integration on Vercel):
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
